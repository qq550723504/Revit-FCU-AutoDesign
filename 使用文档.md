# FCUAutoDesign 使用文档

## 1. 功能简介

FCUAutoDesign 是一个 Revit 外接程序，用于在房间内放置风机盘管（FCU），并根据选定的供水、回水主管自动创建连接管线。插件支持下翻避让、三通连接、回水管连接、冷凝水坡度管以及房间面积对应的管径选择。

## 2. 使用环境

- Windows
- Autodesk Revit 2022 或与项目引用版本匹配的 Revit 版本
- .NET Framework 4.8
- 对应版本的 Revit API DLL
- 已编译的 `FCUAutoDesign.dll`

项目当前使用 Revit API 引用。切换 Revit 版本时，需要同步检查项目中的 Revit DLL 路径和 `.addin` 文件目录。

## 3. 编译插件

1. 使用 Visual Studio 打开 `FCUAutoDesign.sln`。
2. 确认项目目标框架为 `.NET Framework 4.8`。
3. 根据本机 Revit 版本检查 `FCUAutoDesign.csproj` 中的 Revit API 引用路径。
4. 选择 `Debug` 或 `Release` 配置并生成项目。
5. 确认生成目录中存在 `FCUAutoDesign.dll`。

也可以使用 MSBuild 生成：

```powershell
msbuild FCUAutoDesign.csproj /p:RevitVersion=2022 /p:RevitInstallPath="C:\Program Files\Autodesk\Revit 2022"
```

## 4. 安装到 Revit

### 4.1 准备 `.addin` 文件

编辑 `FCUAutoDesign.addin`，将 `<Assembly>` 节点修改为本机 DLL 的绝对路径，例如：

```xml
<Assembly>C:\Users\用户名\code\FCUAutoDesign\bin\CondensateOrthogonalDetour\FCUAutoDesign.dll</Assembly>
```

路径中的 DLL 必须真实存在，并且与当前运行的 Revit 版本兼容。

### 4.2 复制插件注册文件

将 `FCUAutoDesign.addin` 复制到对应 Revit 版本的 Addins 目录：

```text
%AppData%\Autodesk\Revit\Addins\2022\
```

如果使用其他 Revit 版本，将目录中的 `2022` 替换为对应版本号。复制完成后重新启动 Revit。

## 5. 使用前准备

运行插件前，请确认当前 Revit 项目满足以下条件：

- 项目中存在已定义边界的房间（Room）。
- 目标房间内或附近存在门（Door）；插件会优先根据门的位置和朝向确定 FCU 放置点。
- 模型中存在可选的供水主管（Pipe）。
- 如果启用回水管功能，还需要存在可选的回水主管（Pipe）。
- 项目中存在可用于放置的机械设备族类型，并且族包含有效的管道连接件。
- 供水、回水和冷凝水连接件的系统分类或描述信息应尽量明确，以便插件识别。

## 6. 操作流程

1. 打开目标 Revit 项目和对应平面视图。
2. 从 Revit 外接程序菜单中启动 FCUAutoDesign 命令。
3. 在参数窗口中填写设计参数，并选择需要启用的功能。
4. 点击“确认参数并开始拾取”。
5. 按提示选择目标房间。
6. 按提示选择走廊供水水平主管。
7. 如果启用了回水主管功能，按提示选择回水水平主管；不需要回水主管时可取消勾选，或在拾取时按 `Esc` 跳过。
8. 插件在当前事务中完成 FCU 放置、管线创建和连接处理。
9. 检查 Revit 模型中的 FCU、供水管、回水管、冷凝水管和连接件状态。

## 7. 参数说明

| 参数 | 默认值 | 说明 |
| --- | ---: | --- |
| 进门过道内缩 | 800 mm | 以房间门位置为基准，沿房间方向计算 FCU 放置点。 |
| 风盘相对标高净高 | 2600 mm | 相对于房间所属标高的高度，插件会叠加房间标高计算绝对标高。 |
| 阀门检修预留段 | 400 mm | FCU 接管后水平方向的预留长度。 |
| 向下翻弯避让高差 | 150 mm | 连接管从 FCU 方向向下翻弯时使用的高差。 |
| 启用房间面积与水力负荷动态选型 | 开启 | 房间面积小于 25 平方米时使用 DN20，否则使用 DN25。 |
| 连接走廊回水主管 | 开启 | 启用后会额外请求选择回水主管并创建回水连接。 |
| 选择冷凝水主管并接入 | 关闭 | 启用后增加主管拾取，创建顺坡支管并打断主管接入三通；失败时回滚整个冷凝水操作。 |
| 冷凝水管型 | 随所选主管 | 无需单独选择，支管沿用主管管型。 |
| 冷凝水系统类型 | 随所选主管 | 主管与设备唯一管道端接口均须为卫生设备（Sanitary）分类；窗口无需另选系统类型。 |
| 供回水主管打断并插入三通 | 开启 | 启用后尝试打断主管并插入三通；失败时使用子事务回滚打断操作。 |

## 8. 管线连接逻辑

- 供水连接会根据 FCU 连接件创建下翻避让支管。
- 回水连接使用与供水类似的连接流程，但需要单独选择回水主管。
- 冷凝水使用与供回水相同的固定路径：设备水平预留、下翻、横向接近主管、竖直接入主管，再打断主管插入三通。冷凝水仍要求 Sanitary 系统分类并使用独立支管管径；不代表已验证排水坡度或重力工况。
- 启用主管打断功能时，插件会在主管投影位置创建断点，并尝试连接主管两端与支管。
- 插件会检查连接件是否已经连接，避免重复占用同一连接件。
- 三通创建失败或连接未闭合时，相关主管打断操作会回滚；请根据 Revit 提示和模型状态检查原因。

## 9. 结果检查

完成操作后，建议检查以下内容：

1. FCU 是否位于目标房间内。
2. FCU 标高是否符合设计要求。
3. 供水、回水管是否连接到正确的主管。
4. 管道之间是否生成弯头或三通。
5. 冷凝水管是否按实际标高连接，管段和管件逐段连通。
6. 管道直径是否符合房间面积对应的设计要求。
7. Revit 的系统浏览器、连接件状态和警告信息是否正常。

## 10. 常见问题

### 10.1 Revit 中看不到插件

- 确认 `.addin` 文件位于当前 Revit 版本对应的 Addins 目录。
- 确认 `<Assembly>` 路径使用绝对路径，且 DLL 文件存在。
- 确认 `FullClassName` 为 `FCUAutoDesign.CmdPlaceFCUAndConnect`。
- 关闭并重新启动 Revit。
- 检查 Revit 与插件引用的 API 版本是否一致。

### 10.2 无法选择房间或主管

插件只接受房间和管道对象。请确认：

- 第一步选择的是 Room。
- 第二步和回水选择步骤选择的是 Pipe。
- 当前视图中目标对象可见且未被其他元素遮挡。

### 10.3 找不到可用的 FCU 族

插件会优先查找机械设备类别中名称或族名包含“风机盘管”“FCU”“FP”或“Fan Coil”的族类型。如果找不到，将尝试使用机械设备类别中的第一个族类型。建议提前载入并激活具有正确管道连接件的 FCU 族。

### 10.4 三通创建失败

可能原因包括：

- 主管或支管的系统类型不匹配。
- 项目中没有配置对应的三通管件族。
- 管径、角度或连接件方向不符合管件规则。
- 主管投影点不在有效管线范围内。

可以先关闭“打断主管并插入三通”，确认 FCU 和支管能够正常放置后，再检查项目的管件配置。

### 10.5 冷凝水管没有生成

请检查冷凝水功能是否启用，无需单独选择管型。拾取阶段选择卫生设备（Sanitary）分类的冷凝水主管，系统名称包含“凝”或“排”不代表分类有效。参考标高使用目标房间标高。

冷凝水建管、弯头或三通失败时，回滚支管及主管打断的整个冷凝水子事务，执行报告的“运行提示”保留具体异常原因。回滚失败、再生成失败或主事务提交失败会终止整次操作。成功提交后还会重新读取并复核支管方向与实际标高及设备到三通和两段主管的完整连接链；失败时回滚整个事务组。

## 11. 注意事项

- 插件会修改当前 Revit 文档，正式模型操作前建议保存文件或先在副本中验证。
- 插件自动处理的是连接和几何布置，不替代专业人员对水力计算、碰撞检查和施工图成果的复核。
- 运行过程中不要同时修改同一批管线或连接件。
- 输入参数应使用毫米和百分数，不要在数值框中输入单位文字。
- 本项目为内部开发与学习用途，具体使用范围请遵循项目所属团队或组织的授权要求。

### 冷凝水主管拾取

供回水与冷凝水均启用时，依次拾取房间、供水主管、回水主管、冷凝水主管，共四步；关闭回水时自动减少一步。所有拾取完成后才开始放置设备。冷凝水拾取时按 ESC 会取消整次操作，不会静默退回局部管段模式。

冷凝水接入必然需要打断所选主管，独立于“供回水主管打断并插入三通”选项。它沿供回水同样的水平预留、下翻、横向和竖直接入路径尝试最多 81 个基础候选；仍冲突时再尝试设备侧左右横移 1～2 个步长并将下翻高度增加 0～4 个步长，合计最多 261 个候选。管件无法安装或与供回水相交时回滚当前候选。连通检查不代表水力计算或整栋排水系统验收通过。

### 供回水之间的避让

供水生成后，回水先尝试原预留长度和下翻高度。如果回水无法接入或与供水管道、弯头、三通及打断后的主管相交，则回滚该候选，增加回水预留长度或下翻高度重试。步长为 100 mm 与四倍支管公称直径的较大值，两项分别最多增加四个步长，共最多 25 条候选；报告显示最终采用的尺寸。

所有候选都失败时回滚整次操作，不保留重叠管线。主事务提交后再次执行供回水实体干涉检查。本检查使用 Revit 干涉过滤器，不代表保温层净距、阀门检修空间或与其他模型构件的碰撞检查通过。已有插件生成的重叠管线不会被自动删除，请在干净的模型副本中重新验证。

冷凝水拾取采用点击后校验：分类不符合时显示元素 ID、系统类型和实际分类，并允许重选；链接模型、保温层或管件也会提示不支持的原因。系统分类校验仍然保留。


冷凝水无坡度输入，也不校验设计坡度；水平连接或连通成功不代表重力排水性能达标。

冷凝水不做坡度或高低方向验证：所选主管可以高于、低于或等于设备接口，路径按实际标高连接。仅保留管段长度、规划方向及连通性验证，不保证重力排水能力。


冷凝水主管和设备接口统一采用卫生设备（Sanitary）规则，不接受 OtherPipe、Global 或 UndefinedSystemType。代码规则不会自动修改项目管道或 FCU 族中的分类；报告仍显示实际读取值。

设备接口通过 Revit ConnectTo 自动生成过渡管件时，连接链会包含该管件；仅接受本次新建且直接连接指定两端的唯一两端管件。若仍失败，报告会显示具体相邻元素断点、端接口数量或三通未接到的主管段。诊断 ID 是回滚前记录，新建失败元素回滚后不存在。

三通接入时 Revit 自动生成的过渡管件也计入连接链：支管侧记录在支管与三通之间，两段主管侧分别记录并验证。仅接受本次新建的唯一两端管件，不绕行已有管网。

### 冷凝水与供回水避让

冷凝水生成后检查其与本次供回水管道、管件、三通、过渡管件及两段主管的实体相交。相交或接管失败时完整回滚冷凝水候选，重新读取接口和主管再尝试，保留已生成供回水。

最多 261 条候选：先将预留长度和下翻高度分别由界面参数起增加 0～8 个步长；仍冲突时，在设备侧左右横移 1～2 个步长并将下翻高度增加 0～4 个步长。步长为 100 mm 与四倍支管公称直径的较大值。报告显示成功候选的参数；全部失败则不保留冷凝水管并报告具体原因。此功能不恢复坡度或重力方向限制。

横移候选顺序为“水平预留 → 设备侧横移 → 下翻 → 横向接近主管 → 竖直接入”。横移安排在下翻前，避免与主管接近段共线或折返；按横移后的点定位可用主管段。创建管道前检查各段长度及直角转折。弯头失败会报告第几个弯头、相邻段位和管型；全部候选失败时同时列出首条及最后原因，避免最后一次错误掩盖初始问题。

提交后复核连通性和三路之间实体相交；复核失败回滚整次事务组。此检查不包括保温层、检修净距及其他模型构件。原先已生成的相交管线不会被自动清理，复测应使用干净模型副本。

## 多房间批量布置

第一步可连续选择多个同楼层房间，点击 Revit 选取栏的“完成”结束选择。重复房间只处理一次。随后统一选取供水、回水和冷凝水主管（后两者按开关显示），全部选择结束后开始逐房间执行。

第一版限制：房间须同一标高，每间有唯一关联门及直墙，所选主管参考标高与房间一致。各回路只使用最初所选主管及本批次从它打断出的管段，不会自动延伸主管或沿整栋管网搜索。房间跨楼层会在执行前拒绝；单间无门、多门或几何不满足时，该房间失败并回滚，继续处理其他房间。

后续房间接入前重新定位可用主管段；落在已有三通间隙、端点附近或主管范围外的候选会失败。供水、回水、冷凝水的候选路线均检查与前面房间已生成支管及管件的实体相交。跨房间检查排除共用主管本体，仍包含三通与自动过渡管件。新房间提交前还会复核先前房间的连接链，避免后续打断破坏已有接入。

每房间独立事务组。只有成功提交的房间才登记主管新分段；失败候选不会污染后续房间。若某房间仅完成部分启用回路，报告“部分完成”，保留已经验证的部分。用户中止执行或发生模型再生成失败时停止后续房间，标记“未执行”。汇总窗口列出连接完成、部分完成、失败及未执行数量，展开详细信息可查看每房间 FCU ID、回路状态和原因。

本批次不会自动清理以前运行创建的设备或管线。批量集成与真实管件适配需在隔离模型副本内验证。
