# FCUAutoDesign 使用文档

## 1. 当前版本定位

FCUAutoDesign 是面向 Autodesk Revit 2020 的同楼层多房间 FCU 布置与接管验证版，目标框架为 .NET Framework 4.8。

当前版本可以：

- 按房间关联门及门所在直墙确定局部方向。
- 在门侧墙室内侧指定距离处布置一台或多台 FCU。
- 连接供水、可选回水和可选冷凝水主管。
- 自动发现当前平面视图内符合名称关键词的房间及其可唯一归属的主管区域。
- 保存每个成功房间的设计记录，并对已有记录执行受限的修改重算。
- 使用 AI 方案助手把自然语言转换为表单参数。

当前版本不提供：

- 正式设备型号自动匹配。
- 正式水力选径或水力性能验算。
- 冷凝水坡度和重力排水验算。
- 全模型、链接模型、保温、套管、检修空间或施工净距检查。
- 洞口或套管创建。
- 已有设备、位置、数量、族类型和管线几何的自动重建。

业务预览中的型号只用于计算预览。实际放置始终使用用户在窗口中选择的 Revit 族类型；支管采用设备实际圆形接口尺寸。

## 2. 运行环境

- Windows
- Autodesk Revit 2020
- .NET Framework 4.8
- 与 Revit 2020 API 编译的 `FCUAutoDesign.dll`

其他 Revit 版本尚未验证，不能直接复用 Revit 2020 程序集。

## 3. 安装

1. 完全退出 Revit。
2. 将以下两个文件复制到 `%APPDATA%\Autodesk\Revit\Addins\2020\`：
   - `FCUAutoDesign.dll`
   - `FCUAutoDesign.addin`
3. 确认 `.addin` 中为相对程序集路径：

```xml
<Assembly>FCUAutoDesign.dll</Assembly>
```

4. 启动 Revit 2020，在“外部工具”中运行 FCU 自动布置与接管命令。

不要在 Revit 运行时覆盖 DLL。每次更新程序集后必须重新启动 Revit。

## 4. 模型准备

### 4.1 房间和门

- 所选房间必须在同一楼层，具有有效标高、封闭面积和可读取边界。
- 当前业务规则只识别由直线边界形成的矩形局部范围；弧形或非矩形边界不会使用世界坐标包围盒替代。
- 房间必须关联门，门必须宿主在水平直墙上。
- 门所在墙方向定义 L 轴，垂直方向定义 H 轴；L 不按房间长边猜测。
- 多个门位于同一直墙边时自动选择最靠近房间中心的门；位于不同墙边时需要人工拾取目标门。

### 4.2 FCU 族

- 族类别必须是“机械设备”。
- 族名或类型名应包含“风机盘管”“FCU”“FP”或“Fan Coil”。
- 族必须具有唯一、明确分类的供水接口；启用回水或冷凝水时还应具有对应系统分类的管道接口。
- 送风接口必须为水平出风，插件使用实际送风接口方向校正设备朝向。

### 4.3 主管

- 主管参考标高必须与目标房间楼层一致。
- 当前接管只支持水平直线主管或由已连接水平直线段组成的主管段组。
- 供水主管必须是 `SupplyHydronic`，回水主管必须是 `ReturnHydronic`。
- 冷凝水主管必须是 `Sanitary` 系统分类。
- 项目管道类型必须配置与实际管径和转角匹配的弯头、三通等 Routing Preferences。

## 5. 参数说明

| 参数 | 默认值 | 当前含义 |
| --- | ---: | --- |
| 操作模式 | 首次生成 | 首次生成只处理没有插件设计记录的房间；修改重算只处理已有记录的房间。 |
| FCU 族类型 | 必选 | 实际放置的 Revit 机械设备类型，不会被业务预览建议型号自动替换。 |
| 距门侧墙内侧距离 | 500 mm | 从门宿主墙室内表面向房间内偏移，沿门墙方向取房间局部中部。 |
| 冷指标 | 200 W/m² | 用于负荷和台数预览；必须大于零才执行。它不是项目已确认的正式工程参数。 |
| 风盘相对标高净高 | 2600 mm | 相对房间所属标高的设备安装高度。 |
| 接管目标距离 | 400 mm | 设备接口至首个转弯中心的目标距离，是试用默认值，不是规范最小值。 |
| 最小净直管 | 空 | 根据设备、管件和安装详图填写；填写后必须小于接管目标距离。留空表示工程安装净长未校验。 |
| 避让下翻起始高差 | 150 mm | 无下翻候选不可用时开始尝试的下翻高差。 |
| 自动发现目标范围 | 关闭 | 在当前平面视图、当前标高按房间名称关键词发现房间和可唯一归属的主管区域。 |
| 房间名称关键词 | 会议室；办公室 | 房间名称包含任一关键词即为候选，不按房间类型参数筛选。 |
| 正式自动选径 | 禁用 | 等待正式水力表及匹配依据确认。 |
| 业务规则预览 | 开启 | 执行前显示负荷、台数、建议型号、点位和自动范围；建议型号不替换族类型。 |
| 多台均布 | 开启 | 台数按 L/5 向上取整，多台沿门侧墙方向等距布置，两端留半间距。 |
| 连接回水主管 | 开启 | 连接设备回水接口；手动拾取时可按 `Esc` 跳过回水。 |
| 连接冷凝水主管 | 关闭 | 连接 `Sanitary` 主管，只校验模型连通，不校验坡度和重力方向。 |
| 供回水主管打断并插入三通 | 开启 | 供回水接入主管；关闭时只生成支管，不保证主管连接完成。冷凝水接入仍需要三通。 |

## 6. AI 方案助手

AI 方案助手把自然语言转换为候选表单参数。例如：

> 冷指标 200 W/㎡，为所有办公室和会议室多台均布，距门侧墙 500 mm，安装高度 2500 mm，连接供回水主管。

使用步骤：

1. 输入要求并点击“生成方案”。
2. 阅读摘要、参数、警告、未执行事项和澄清项。
3. 没有澄清项时点击“应用方案”。
4. 复核窗口参数，再点击“确认参数并确定范围”。

AI 不读取尚未选择的模型元素，不直接修改 Revit，不决定正式型号或正式管径。“应用方案”只回填表单。仅诊断方案和含澄清项的方案不能回填。

API Key 使用当前 Windows 用户环境变量，不写入仓库、`.addin` 或 DLL：

```powershell
setx FCU_AGENT_API_KEY "替换为客户自己的密钥"
```

可选覆盖兼容接口和模型：

```powershell
setx FCU_AGENT_BASE_URL "https://example.com/v1"
setx FCU_AGENT_MODEL "兼容模型名称"
```

设置后完全退出并重新启动 Revit。不要把真实密钥放入测试包、配置文件、日志或截图。

## 7. 执行流程

### 7.1 首次生成

1. 打开具有标高的目标楼层平面视图。
2. 打开插件，选择 FCU 族类型并检查参数。
3. 选择手动范围或启用自动发现。
4. 点击“确认参数并确定范围”。
5. 手动模式依次拾取房间、供水主管以及已启用的回水和冷凝水主管；自动模式只对能够唯一确定主管区域的房间执行，无法覆盖或归属不唯一的房间会被排除。
6. 多门且不在同一墙边的房间需要逐间拾取目标门。
7. 在“FCU 综合执行预览”中检查房间、参数、族类型、主管区域和排除原因。
8. 确认后才进入 Revit 事务。

自动发现成功时不再分别弹出房间和主管确认框。自动发现完全失败时会说明原因并退回手动拾取。

### 7.2 修改重算

修改重算只读取具有插件设计记录的房间，并对“上次成功快照、当前模型、期望方案”进行差异预览。

当前可写范围仅包括冷指标和设计负荷记录。只有纯计算字段变化且没有混入新房间、设备几何、型号、数量或管线变化时，用户确认后才更新设计记录和 Revision。其他差异只读展示，不移动或替换设备，不修改管线和管径。

## 8. 布置与接管规则

- FCU 基线位于门侧墙室内侧指定距离处，不使用房间几何中心。
- 单台位于门墙方向的局部中部；多台沿 L 轴等距布置。
- 所有候选点必须位于房间体积内；不满足时整间不放置。
- 三路接管先尝试不下翻，再尝试下翻和有限横移候选。
- 供回水及冷凝水支管分别采用设备对应接口的实际圆形尺寸。
- 最小净直管填写后会在管件安装和提交复核阶段检查；留空不代表满足安装要求。
- 宿主直墙只允许水平正交穿越，并检查本次管段和管件是否停留墙内或擦墙。
- 冷凝水有有限候选和试建预算；达到预算表示停止继续搜索，不代表所有可能路线均不可行。

## 9. 事务、回滚和批量结果

- 每个房间由独立事务组保护。
- 同一房间多台 FCU 中任一台接管未完成，整间新增设备和管线回滚。
- 一个普通房间失败后，其他房间可以继续执行。
- 再生成失败、用户取消或冷凝水明确未完成时会停止后续房间。
- 已成功提交的前序房间不会因普通后续房间失败而撤销。
- 成功房间保存设计身份、版本和上次成功快照，用于重复生成保护和修改重算。

结果窗口区分“连接完成、部分完成、失败（已回滚）、未执行”。详细信息会列出设备数量、元素 ID、各回路状态、警告、执行耗时和诊断文件路径。

## 10. 验收边界

自动化检查覆盖独立业务计算、几何候选、连接结果、设计记录、差异计划、AI 契约和窗口参数。以下项目仍需在客户 Revit 2020 模型副本中验证：

- 真实族接口和项目 Routing Preferences 下的弯头、三通安装。
- 多房间、多主管区域以及不同门墙条件下的实际布置。
- 墙体、现有 MEP、链接模型和施工净距条件下的可施工性。
- 冷凝水坡度与重力排水性能。
- 正式水力表、设备目录与族类型映射。

模型连通成功不等于水力、排水或施工验收通过。

## 11. 常见问题

### 找不到 FCU 类型

先载入机械设备族，并确认族名或类型名包含“风机盘管”“FCU”“FP”或“Fan Coil”。检查族的供水、回水、冷凝水和送风接口分类及方向。

### 自动发现没有选中房间

确认当前视图是具有标高的平面视图，房间位于该标高、具有封闭面积，名称包含配置关键词，并且门和矩形局部边界可以识别。首次生成会跳过已有设计记录的房间，修改重算会跳过没有记录的房间。

### 自动主管归属失败

主管必须在当前视图可见、与房间同一参考标高、水平且系统分类正确。一个房间同时被零条或多条候选主管段组覆盖时不会自动决定，会排除该房间或退回手动选择。

### 弯头或三通失败

检查管径、系统类型、角度和管道类型的 Routing Preferences。查看结果中的第一条和最后一条失败原因及 `%LOCALAPPDATA%\FCUAutoDesign\Diagnostics\` 下的诊断文件。

### 已有设计无法再次生成

首次生成会阻止重复放置。切换“修改重算”查看差异；当前仅支持安全更新冷指标和设计负荷记录，几何和管线变化仍需人工处理。

### AI 不可用

确认 `FCU_AGENT_API_KEY` 已在当前 Windows 用户环境中设置，兼容接口使用 HTTPS，模型名称有效，并在设置环境变量后重启 Revit。

## 12. 编译验证

```powershell
& 'C:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe' `
  FCUAutoDesign.csproj /t:Rebuild /p:Configuration=Release /p:Platform=x64 `
  /p:RevitVersion=2020 /nologo /verbosity:minimal
```

本地自动化通过不代表 Revit 模型验收完成。修改程序集后应重新复制 DLL，重启 Revit，并在模型副本中验证。
